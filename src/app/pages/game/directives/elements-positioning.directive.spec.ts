import { ElementsPositioningDirective } from './elements-positioning.directive';

describe('ElementsPositioningDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementsPositioningDirective();
    expect(directive).toBeTruthy();
  });
});
